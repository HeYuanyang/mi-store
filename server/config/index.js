/**
 * 配置文件
 */

const config = {
  // 服务器端口
  port: 3000,
  // 数据库配置
  database: {
    host: 'localhost',
    port: '3306',
    user: 'root',
    password: 'yang516624',
    database: 'mi_store_db'
  }
}

module.exports = config
